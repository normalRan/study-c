#include <stdio.h>
#include <stdlib.h>

void Bin_Sort(int A[], int N) {
	if (N == 1)
		return;
}

int main() {
/*	int A[];	 
	int N = sizeof(A)/sizeof(A[0]);
	Bin_Sort(A, N)
	int i;
	for (i=0; i<N; i++)
		printf("%3d", A[i]);
	printf("\n"); */
}
